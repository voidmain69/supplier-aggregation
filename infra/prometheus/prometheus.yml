global:
  scrape_interval: 15s

# Metrics arrive via remote-write from the OTel collector; no scrape targets needed for services.
scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
