[project]
name = "supplier-aggregation"
version = "0.1.0"
description = "Multi-supplier product aggregation platform (workspace root)"
requires-python = ">=3.12,<3.13"

[tool.uv.workspace]
members = ["libs/*", "services/*"]

[dependency-groups]
dev = [
    "ruff>=0.5",
    "mypy>=1.10",
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5",
    "import-linter>=2",
    "pre-commit>=3.7",
    "jsonschema>=4.22",
    "datamodel-code-generator>=0.68",   # `make contracts`: models from contracts/events/*.json
    "httpx>=0.27",                      # FastAPI TestClient + httpx instrumentation in tests
    "opentelemetry-instrumentation-httpx>=0.46b0",
]

# ---------------------------------------------------------------------------
# Ruff — single config for the whole monorepo
# ---------------------------------------------------------------------------
[tool.ruff]
line-length = 100
target-version = "py312"
src = ["libs", "services", "tools"]

[tool.ruff.lint]
select = [
    "E", "W",      # pycodestyle
    "F",           # pyflakes
    "I",           # isort
    "B",           # bugbear
    "C4",          # comprehensions
    "UP",          # pyupgrade
    "S",           # bandit (security)
    "T20",         # no print
    "DTZ",         # naive datetime forbidden
    "ASYNC",       # async pitfalls
    "RUF",
    "PL",          # pylint subset
]
ignore = [
    "PLR0913",     # too many args — Pydantic models/DI factories trigger this
    "PLR2004",     # magic value in comparison — too noisy for tests
]

[tool.ruff.lint.per-file-ignores]
"**/tests/**" = ["S101", "S105", "S106", "PLR"]   # assert + dummy creds allowed in tests
"tools/**" = ["T20"]                              # CLI tools may print
# Generated from contracts/events/*.json — long lines are verbatim LLM-facing descriptions.
"libs/contracts/src/sa_contracts/events/*" = ["E501"]
"libs/contracts/src/sa_contracts/registry.py" = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["sa_core", "sa_contracts", "sa_connector_sdk", "sa_observability"]

# ---------------------------------------------------------------------------
# Mypy
# ---------------------------------------------------------------------------
[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true
show_error_codes = true
plugins = ["pydantic.mypy"]
# src-layout packages: each package's src/ dir is a module base so files resolve to
# their real dotted name (libs/core/src/sa_core/ids.py -> sa_core.ids). Extend this list
# when a new lib/service is added.
explicit_package_bases = true
mypy_path = [
    "libs/core/src",
    "libs/contracts/src",
    "libs/observability/src",
    "libs/connector-sdk/src",
]
exclude = ["/tests/", "/migrations/"]

[[tool.mypy.overrides]]
# adapters may talk to untyped third-party SDKs; keep the leak contained there
module = "*.adapters.*"
disallow_untyped_calls = false
warn_return_any = false

# ---------------------------------------------------------------------------
# Pytest
# ---------------------------------------------------------------------------
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["libs", "services", "tools"]
markers = [
    "integration: needs docker (testcontainers)",
    "live: hits a real supplier API — never run in CI",
]
# importlib mode lets same-named test files (test_errors.py) coexist across packages.
addopts = "-m 'not live' --strict-markers --import-mode=importlib"

[tool.coverage.run]
branch = true
source = ["libs", "services"]
omit = ["**/tests/**", "**/migrations/**"]

# ---------------------------------------------------------------------------
# Import-linter — architectural boundaries
# ---------------------------------------------------------------------------
# List packages here as they are created (sa_connector_sdk, sa_observability, …).
[tool.importlinter]
root_packages = ["sa_core", "sa_contracts", "sa_observability", "sa_connector_sdk"]
include_external_packages = true

[[tool.importlinter.contracts]]
name = "libs must not depend on services"
type = "forbidden"
source_modules = ["sa_core", "sa_contracts", "sa_observability", "sa_connector_sdk"]
forbidden_modules = ["services"]

[[tool.importlinter.contracts]]
name = "core is the lowest layer (must not import other sa libs)"
type = "forbidden"
source_modules = ["sa_core"]
forbidden_modules = ["sa_contracts", "sa_observability", "sa_connector_sdk"]

[[tool.importlinter.contracts]]
name = "contracts must not depend on observability"
type = "forbidden"
source_modules = ["sa_contracts"]
forbidden_modules = ["sa_observability"]

[[tool.importlinter.contracts]]
name = "connector-sdk depends only on core (not contracts/observability)"
type = "forbidden"
source_modules = ["sa_connector_sdk"]
forbidden_modules = ["sa_contracts", "sa_observability"]
