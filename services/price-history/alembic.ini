# Alembic config for the price-history service. The DSN comes from the service settings
# (env-overridable, e.g. PRICE_HISTORY_DB_DSN) — see migrations/env.py — not from here.
[alembic]
script_location = %(here)s/migrations
prepend_sys_path = .
path_separator = os
